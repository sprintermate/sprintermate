# ── Backend .env — used by `npm run dev` ──────────────────────────────────────
# Copy this file to backend/.env for local development.

# ── Server ─────────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=4000

# ── Database ───────────────────────────────────────────────────────────────────
# Select the database backend: "sqlite" (default) or "postgres"
DB_CHOICE=sqlite

# SQLite (used when DB_CHOICE=sqlite)
# Path to the SQLite database file (relative to backend/ directory)
DB_PATH=data/sprintermate.db

# PostgreSQL (required when DB_CHOICE=postgres)
# POSTGRES_DB_HOST=localhost
# POSTGRES_DB_PORT=5432
# POSTGRES_DB_NAME=sprintermate
# POSTGRES_DB_USER=postgres
# POSTGRES_DB_PASSWORD=change-me

# ── Security secrets [REQUIRED] ────────────────────────────────────────────────
# Generate strong secrets with: openssl rand -hex 32

# Secret for signing JWT tokens
JWT_SECRET=change-this-to-a-long-random-string-in-production

# AES-256-GCM key for encrypting stored PATs and API keys
# WARNING: changing this will invalidate all existing encrypted secrets in the DB
ENCRYPTION_KEY=change-this-to-a-long-random-string-in-production

# ── CORS ───────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
FRONTEND_URL=http://localhost:3000

# ── Cookie security ─────────────────────────────────────────────────────────────
# NODE_ENV=production enables secure=true cookies (requires HTTPS).
# Leave unset or set to "development" for local HTTP access.
# NODE_ENV=development

# ── Production AI ───────────────────────────────────────────────────────────────
# NODE_ENV=production only — user-configured providers are ignored when set
# GEMINI_API_KEY=your-google-ai-studio-api-key
